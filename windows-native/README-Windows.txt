417ssh Windows 0.6.0

1. 解压整个 417ssh 文件夹。
2. 双击 417ssh.exe 运行。
3. 不要把 417ssh.exe 单独移出文件夹。

配置文件保存在：
%APPDATA%\417ssh\profiles.json

0.6.0 Windows 版本改为原生 WPF/.NET 实现，不再使用 Python/PySide6/QtWebEngine 打包。
